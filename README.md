# Materials on the article
## what's ready: RUL review, STAR baseline, Overleaf (basic article structure), Project structure, baseline model weights in releases 
# Launching the inference:
Place the trained weights of the model (file .pth) go to the artifacts/checkpoints/ folder and run the evaluation script:
